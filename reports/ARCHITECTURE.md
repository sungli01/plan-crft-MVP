# Plan-Craft 기술 아키텍처

> 🗓️ 작성일: 2026-02-08  
> 📐 버전: v3.0  
> ✍️ 작성자: 바질 (AI 어시스턴트)

---

## 📐 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 (브라우저)                          │
└─────────────────────┬───────────────────────────────────────────┘
                      │ HTTPS
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                    프론트엔드 (Vercel)                             │
│              Next.js 14 App Router                               │
│              https://plan-crft-mvp-ot41.vercel.app               │
│                                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐   │
│  │ 입력 폼   │ │ 진행 추적 │ │ 결과 뷰어 │ │ 공통 컴포넌트      │   │
│  │ (Create) │ │(Progress)│ │ (Viewer) │ │(Header,Toast,등) │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘   │
└─────────────────────┬───────────────────────────────────────────┘
                      │ REST API (JSON)
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                    백엔드 (Railway)                               │
│              Hono Framework                                      │
│              https://plan-crft-mvp-production.up.railway.app     │
│                                                                   │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │ Auth       │ │ Plans API  │ │ Rate Limit │ │ Error      │   │
│  │ Middleware │ │ Routes     │ │ Middleware │ │ Handler    │   │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              AI 에이전트 엔진 (Claude Opus 4.6)           │    │
│  │                                                          │    │
│  │  ① Architect ──→ ② Writer ──→ ③ Image Curator ──→ ④ Reviewer │
│  │  (구조 설계)     (콘텐츠)      (이미지 선별)       (품질 검토)  │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────┬───────────────────────────────────────────┘
                      │ Drizzle ORM
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                   데이터베이스 (Railway)                           │
│                   PostgreSQL                                     │
│                                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐   │
│  │ users    │ │ plans    │ │ sections │ │ generation_logs  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 백엔드 아키텍처

### 기술 스택

| 기술 | 버전 | 용도 |
|------|------|------|
| **Hono** | 최신 | 웹 프레임워크 (경량, Edge 호환) |
| **PostgreSQL** | 15+ | 관계형 데이터베이스 |
| **Drizzle ORM** | 최신 | TypeScript ORM (타입 안전) |
| **JWT** | - | 사용자 인증 토큰 |
| **Claude Opus 4.6** | 4.6 | AI 사업계획서 생성 |

### 디렉토리 구조

```
backend/
├── src/
│   ├── index.ts              # 앱 진입점, Hono 서버 설정
│   ├── routes/
│   │   ├── auth.ts           # 인증 관련 라우트
│   │   ├── plans.ts          # 사업계획서 CRUD 라우트
│   │   └── health.ts         # 헬스체크 엔드포인트
│   ├── middleware/
│   │   ├── auth.ts           # JWT 인증 미들웨어
│   │   ├── rateLimit.ts      # Rate Limiting 미들웨어
│   │   ├── cors.ts           # CORS 설정
│   │   └── errorHandler.ts   # 전역 에러 핸들러
│   ├── services/
│   │   ├── aiEngine.ts       # AI 에이전트 오케스트레이션
│   │   ├── architect.ts      # Architect 에이전트
│   │   ├── writer.ts         # Writer 에이전트
│   │   ├── imageCurator.ts   # Image Curator 에이전트
│   │   └── reviewer.ts       # Reviewer 에이전트
│   ├── db/
│   │   ├── schema.ts         # Drizzle 스키마 정의
│   │   ├── migrations/       # DB 마이그레이션 파일
│   │   └── index.ts          # DB 연결 설정
│   └── types/
│       └── shared.ts         # 공유 TypeScript 타입
├── drizzle.config.ts         # Drizzle 설정
├── package.json
└── tsconfig.json
```

### Hono 프레임워크

Hono는 **경량 웹 프레임워크**로, Express와 유사한 API를 제공하면서도 훨씬 빠르고 가볍습니다.

```typescript
// src/index.ts 예시 구조
import { Hono } from 'hono'
import { cors } from 'hono/cors'
import { authRoutes } from './routes/auth'
import { planRoutes } from './routes/plans'
import { authMiddleware } from './middleware/auth'
import { rateLimiter } from './middleware/rateLimit'
import { errorHandler } from './middleware/errorHandler'

const app = new Hono()

// 전역 미들웨어
app.use('*', cors())
app.use('*', errorHandler)
app.use('/api/*', rateLimiter)

// 라우트
app.route('/api/auth', authRoutes)
app.route('/api/plans', authMiddleware, planRoutes)

// 헬스체크
app.get('/health', (c) => c.json({ status: 'ok' }))

export default app
```

### JWT 인증 시스템

```
┌──────────┐     POST /api/auth/login     ┌──────────┐
│  클라이언트 │ ─────────────────────────→ │  서버      │
│          │                               │          │
│          │  ← JWT 토큰 반환               │          │
│          │                               │          │
│          │  Authorization: Bearer <JWT>   │          │
│          │ ─────────────────────────→     │  검증     │
│          │                               │  ✓ 통과   │
│          │  ← API 응답                    │          │
└──────────┘                               └──────────┘
```

### Rate Limiting

- **윈도우**: 15분
- **최대 요청**: 100회 (일반 API), 10회 (사업계획서 생성)
- **초과 시**: HTTP 429 Too Many Requests 반환
- **목적**: API 남용 방지, Claude API 비용 제어

### 4-에이전트 AI 엔진

사업계획서 생성은 4개의 전문 에이전트가 **파이프라인 방식**으로 순차 실행합니다.

```
입력: 사업 아이디어 (텍스트)
         │
         ▼
┌─────────────────┐
│ ① Architect      │  구조 설계 에이전트
│                  │  - 사업 아이디어 분석
│                  │  - 목차 및 섹션 구조 설계
│                  │  - 각 섹션의 핵심 포인트 정의
│                  │  출력: 목차 + 섹션 구조 (JSON)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ② Writer         │  콘텐츠 작성 에이전트
│                  │  - 각 섹션별 상세 내용 작성
│                  │  - 시장 분석, 재무 계획 등 포함
│                  │  - 전문적이고 설득력 있는 문체
│                  │  출력: 25개 섹션 콘텐츠 (10,800+ 단어)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ③ Image Curator  │  이미지 큐레이션 에이전트
│                  │  - 각 섹션에 적합한 이미지 선별
│                  │  - 차트, 그래프, 인포그래픽 배치
│                  │  - 시각적 완성도 향상
│                  │  출력: 93개 이미지 참조 + 배치 정보
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ④ Reviewer       │  품질 검토 에이전트
│                  │  - 전체 문서 품질 검토
│                  │  - 논리적 일관성 확인
│                  │  - 점수 산출 (0-100)
│                  │  출력: 품질 점수 + 개선 제안
└────────┬────────┘
         │
         ▼
출력: 완성된 사업계획서 (87.6/100)
```

---

## 🎨 프론트엔드 아키텍처

### 기술 스택

| 기술 | 버전 | 용도 |
|------|------|------|
| **Next.js** | 14 | React 프레임워크 (App Router) |
| **Tailwind CSS** | 3.x | 유틸리티 기반 CSS |
| **TypeScript** | 5.x | 타입 안전한 JavaScript |
| **next-themes** | - | 다크모드 지원 |

### 디렉토리 구조

```
frontend/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx          # 루트 레이아웃 (ThemeProvider 포함)
│   │   ├── page.tsx            # 홈페이지 (랜딩)
│   │   ├── create/
│   │   │   └── page.tsx        # 사업계획서 생성 페이지
│   │   ├── plans/
│   │   │   ├── page.tsx        # 사업계획서 목록
│   │   │   └── [id]/
│   │   │       └── page.tsx    # 사업계획서 상세 뷰어
│   │   ├── progress/
│   │   │   └── [id]/
│   │   │       └── page.tsx    # 실시간 진행 추적
│   │   └── auth/
│   │       ├── login/
│   │       │   └── page.tsx    # 로그인 페이지
│   │       └── register/
│   │           └── page.tsx    # 회원가입 페이지
│   ├── components/
│   │   ├── common/
│   │   │   ├── Header.tsx      # 공통 헤더 (네비게이션)
│   │   │   ├── Toast.tsx       # 토스트 알림 시스템
│   │   │   ├── Skeleton.tsx    # 로딩 스켈레톤 UI
│   │   │   └── ThemeProvider.tsx # 다크모드 제공자
│   │   ├── plan/
│   │   │   ├── PlanForm.tsx    # 사업 아이디어 입력 폼
│   │   │   ├── PlanViewer.tsx  # 사업계획서 뷰어
│   │   │   ├── PlanCard.tsx    # 사업계획서 카드 (목록용)
│   │   │   └── ProgressTracker.tsx # 실시간 진행 표시기
│   │   └── ui/
│   │       ├── Button.tsx      # 공통 버튼 컴포넌트
│   │       ├── Input.tsx       # 공통 입력 컴포넌트
│   │       └── Modal.tsx       # 공통 모달 컴포넌트
│   ├── lib/
│   │   ├── api.ts              # 중앙집중 API 클라이언트
│   │   └── utils.ts            # 유틸리티 함수
│   ├── types/
│   │   └── shared.ts           # 공유 TypeScript 타입
│   └── styles/
│       └── globals.css         # 전역 스타일 (Tailwind)
├── public/
│   └── ...                     # 정적 파일
├── next.config.js
├── tailwind.config.js
├── package.json
└── tsconfig.json
```

### 공통 컴포넌트 시스템

#### Header

```
┌─────────────────────────────────────────────────────┐
│  🏗️ Plan-Craft          [홈] [생성] [내 계획서]  🌙/☀️ │
└─────────────────────────────────────────────────────┘
```

- 반응형 네비게이션
- 다크모드 토글 버튼
- 로그인 상태에 따른 메뉴 변경

#### Toast 알림 시스템

```
┌────────────────────────────┐
│ ✅ 사업계획서가 생성되었습니다! │  ← 성공 (초록)
└────────────────────────────┘
┌────────────────────────────┐
│ ❌ 오류가 발생했습니다.       │  ← 에러 (빨강)
└────────────────────────────┘
┌────────────────────────────┐
│ ℹ️ 생성 중입니다. 잠시만...   │  ← 정보 (파랑)
└────────────────────────────┘
```

- 자동 소멸 (3~5초)
- 여러 토스트 스택 지원
- 애니메이션 효과

#### Skeleton 로딩 UI

데이터 로딩 중 표시되는 플레이스홀더 UI:
- 텍스트 블록 스켈레톤
- 카드 스켈레톤
- 이미지 플레이스홀더
- 부드러운 펄스 애니메이션

#### ThemeProvider (다크모드)

- `next-themes` 기반
- 시스템 설정 자동 감지
- 수동 토글 지원
- `localStorage`에 사용자 선호 저장
- Tailwind `dark:` 클래스 활용

### 중앙집중 API 클라이언트

```typescript
// lib/api.ts 구조 예시
class ApiClient {
  private baseUrl: string
  private token: string | null

  constructor() {
    this.baseUrl = process.env.NEXT_PUBLIC_API_URL
    this.token = null
  }

  // 공통 요청 메서드
  private async request<T>(endpoint: string, options?: RequestInit): Promise<T> {
    const headers: HeadersInit = {
      'Content-Type': 'application/json',
      ...(this.token && { Authorization: `Bearer ${this.token}` }),
    }
    const res = await fetch(`${this.baseUrl}${endpoint}`, { ...options, headers })
    if (!res.ok) throw new ApiError(res.status, await res.text())
    return res.json()
  }

  // 인증
  login(email: string, password: string) { ... }
  register(email: string, password: string) { ... }

  // 사업계획서
  createPlan(idea: string) { ... }
  getPlan(id: string) { ... }
  getPlans() { ... }
  getPlanProgress(id: string) { ... }
  exportPdf(id: string) { ... }
}

export const api = new ApiClient()
```

### 공유 TypeScript 타입

프론트엔드와 백엔드에서 동일한 타입을 사용하여 **타입 안전성**을 보장합니다:

```typescript
// types/shared.ts
export interface Plan {
  id: string
  userId: string
  idea: string
  title: string
  status: 'pending' | 'generating' | 'completed' | 'failed'
  qualityScore: number | null
  sections: Section[]
  createdAt: string
  updatedAt: string
}

export interface Section {
  id: string
  planId: string
  title: string
  content: string
  order: number
  images: ImageRef[]
}

export interface ImageRef {
  url: string
  alt: string
  caption: string
}

export interface GenerationProgress {
  planId: string
  currentAgent: 'architect' | 'writer' | 'imageCurator' | 'reviewer'
  progress: number       // 0-100
  message: string
  estimatedTimeLeft: number  // 초 단위
}
```

---

## 🌐 API 엔드포인트

### 인증 API

| 메서드 | 엔드포인트 | 설명 | 인증 |
|--------|-----------|------|------|
| `POST` | `/api/auth/register` | 회원가입 | ❌ |
| `POST` | `/api/auth/login` | 로그인 (JWT 발급) | ❌ |
| `GET` | `/api/auth/me` | 현재 사용자 정보 조회 | ✅ |
| `POST` | `/api/auth/refresh` | JWT 토큰 갱신 | ✅ |

### 사업계획서 API

| 메서드 | 엔드포인트 | 설명 | 인증 |
|--------|-----------|------|------|
| `POST` | `/api/plans` | 새 사업계획서 생성 요청 | ✅ |
| `GET` | `/api/plans` | 내 사업계획서 목록 조회 | ✅ |
| `GET` | `/api/plans/:id` | 특정 사업계획서 상세 조회 | ✅ |
| `GET` | `/api/plans/:id/progress` | 생성 진행 상태 조회 | ✅ |
| `GET` | `/api/plans/:id/pdf` | PDF 내보내기 | ✅ |
| `DELETE` | `/api/plans/:id` | 사업계획서 삭제 | ✅ |
| `GET` | `/api/plans/:id/share` | 공유 링크 생성 | ✅ |

### 시스템 API

| 메서드 | 엔드포인트 | 설명 | 인증 |
|--------|-----------|------|------|
| `GET` | `/health` | 서버 헬스체크 | ❌ |

### API 요청/응답 예시

#### 사업계획서 생성 요청

```http
POST /api/plans
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json

{
  "idea": "반려동물 맞춤형 영양제 구독 서비스"
}
```

#### 응답

```json
{
  "id": "plan_abc123",
  "status": "generating",
  "message": "사업계획서 생성을 시작합니다."
}
```

#### 진행 상태 조회

```http
GET /api/plans/plan_abc123/progress
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

#### 응답

```json
{
  "planId": "plan_abc123",
  "currentAgent": "writer",
  "progress": 45,
  "message": "Writer 에이전트가 '시장 분석' 섹션을 작성 중입니다...",
  "estimatedTimeLeft": 120
}
```

---

## 🗄️ 데이터베이스 스키마

### ER 다이어그램

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    users     │       │    plans     │       │   sections   │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ id (PK)      │──┐    │ id (PK)      │──┐    │ id (PK)      │
│ email        │  │    │ userId (FK)  │  │    │ planId (FK)  │
│ password     │  └───→│ idea         │  └───→│ title        │
│ name         │       │ title        │       │ content      │
│ createdAt    │       │ status       │       │ order        │
│ updatedAt    │       │ qualityScore │       │ images       │
└──────────────┘       │ metadata     │       │ createdAt    │
                       │ createdAt    │       └──────────────┘
                       │ updatedAt    │
                       └──────┬───────┘
                              │
                              │
                       ┌──────┴───────┐
                       │generation_logs│
                       ├──────────────┤
                       │ id (PK)      │
                       │ planId (FK)  │
                       │ agent        │
                       │ status       │
                       │ input        │
                       │ output       │
                       │ duration     │
                       │ tokenUsage   │
                       │ createdAt    │
                       └──────────────┘
```

### 테이블 상세

#### `users` — 사용자

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID (PK) | 고유 식별자 |
| `email` | VARCHAR(255) UNIQUE | 이메일 주소 |
| `password` | VARCHAR(255) | bcrypt 해시 비밀번호 |
| `name` | VARCHAR(100) | 사용자 이름 |
| `createdAt` | TIMESTAMP | 가입일 |
| `updatedAt` | TIMESTAMP | 수정일 |

#### `plans` — 사업계획서

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID (PK) | 고유 식별자 |
| `userId` | UUID (FK → users.id) | 소유자 |
| `idea` | TEXT | 원본 사업 아이디어 |
| `title` | VARCHAR(500) | 생성된 사업계획서 제목 |
| `status` | ENUM | pending / generating / completed / failed |
| `qualityScore` | DECIMAL(5,2) | AI 품질 점수 (0-100) |
| `metadata` | JSONB | 기타 메타데이터 |
| `createdAt` | TIMESTAMP | 생성일 |
| `updatedAt` | TIMESTAMP | 수정일 |

#### `sections` — 사업계획서 섹션

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID (PK) | 고유 식별자 |
| `planId` | UUID (FK → plans.id) | 소속 사업계획서 |
| `title` | VARCHAR(500) | 섹션 제목 |
| `content` | TEXT | 섹션 내용 (마크다운) |
| `order` | INTEGER | 정렬 순서 |
| `images` | JSONB | 이미지 참조 배열 |
| `createdAt` | TIMESTAMP | 생성일 |

#### `generation_logs` — 생성 로그

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | UUID (PK) | 고유 식별자 |
| `planId` | UUID (FK → plans.id) | 소속 사업계획서 |
| `agent` | VARCHAR(50) | 에이전트 이름 |
| `status` | ENUM | running / completed / failed |
| `input` | JSONB | 에이전트 입력 데이터 |
| `output` | JSONB | 에이전트 출력 데이터 |
| `duration` | INTEGER | 실행 시간 (ms) |
| `tokenUsage` | JSONB | 토큰 사용량 (input/output) |
| `createdAt` | TIMESTAMP | 생성일 |

---

## 🚀 배포 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                     GitHub Repository                        │
│          https://github.com/sungli01/plan-crft-MVP.git       │
│                                                               │
│  ┌─────────────┐                    ┌─────────────┐         │
│  │  frontend/  │                    │  backend/   │         │
│  └──────┬──────┘                    └──────┬──────┘         │
└─────────┼──────────────────────────────────┼────────────────┘
          │ git push                         │ git push
          │ (자동 배포)                       │ (자동 배포)
          ▼                                  ▼
┌─────────────────┐              ┌─────────────────────┐
│    Vercel        │              │    Railway            │
│                  │              │                       │
│  - Next.js SSR   │              │  - Hono 서버           │
│  - Edge Network  │   ← API →   │  - PostgreSQL DB       │
│  - 글로벌 CDN     │              │  - 자동 스케일링         │
│  - HTTPS 자동     │              │  - HTTPS 자동           │
│  - 프리뷰 배포    │              │  - 헬스체크              │
│                  │              │                       │
│  도메인:          │              │  도메인:                │
│  plan-crft-mvp-  │              │  plan-crft-mvp-        │
│  ot41.vercel.app │              │  production.up.        │
│                  │              │  railway.app           │
└─────────────────┘              └─────────────────────┘
```

### 배포 프로세스

1. **코드 변경** → GitHub에 push
2. **Vercel**: 자동으로 프론트엔드 빌드 및 배포 (약 1~2분)
3. **Railway**: 자동으로 백엔드 빌드 및 배포 (약 2~3분)
4. **프리뷰 배포**: PR 생성 시 Vercel이 자동으로 프리뷰 URL 생성

### 환경 변수 관리

#### 백엔드 (Railway)

```env
DATABASE_URL=postgresql://...
JWT_SECRET=<비밀키>
ANTHROPIC_API_KEY=<Claude API 키>
CORS_ORIGIN=https://plan-crft-mvp-ot41.vercel.app
NODE_ENV=production
PORT=3000
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
```

#### 프론트엔드 (Vercel)

```env
NEXT_PUBLIC_API_URL=https://plan-crft-mvp-production.up.railway.app
NEXT_PUBLIC_APP_NAME=Plan-Craft
```

---

## 🔒 보안 아키텍처

```
사용자 요청
    │
    ▼
┌─────────────┐
│ CORS 검사    │ ← 허용된 origin만 통과
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Rate Limit  │ ← 요청 횟수 제한
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ JWT 인증     │ ← 유효한 토큰만 통과
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 입력 검증    │ ← XSS/Injection 방지
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 비즈니스 로직 │
└─────────────┘
```

### 보안 체크리스트

- [x] HTTPS 강제 (Vercel + Railway 자동)
- [x] CORS 설정 (허용된 도메인만)
- [x] JWT 인증 (만료 시간 설정)
- [x] Rate Limiting (API 남용 방지)
- [x] 환경 변수로 비밀 정보 관리
- [x] bcrypt 비밀번호 해싱
- [x] SQL Injection 방지 (Drizzle ORM 파라미터 바인딩)
- [x] XSS 방지 (React 자동 이스케이프)

---

> 💡 이 문서는 Plan-Craft v3.0의 기술 아키텍처를 설명합니다.  
> 최종 업데이트: 2026-02-08 13:30 KST
